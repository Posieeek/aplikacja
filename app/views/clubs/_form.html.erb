<%= form_with(model: club, local: true) do |form| %>
  <% if club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Nazwa %>
    <%= form.text_field :name, id: :club_name %>
  </div>

  <div class="field">
    <%= form.label :Liga %>
    <%= form.text_field :league, id: :club_league %>
  </div>

  <div class="field">
    <%= form.label :Miejsce %>
    <%= form.number_field :place, id: :club_place %>
  </div>

  <div class="field">
    <%= form.label :Budżet %>
    <%= form.number_field :budget, id: :club_budget %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
