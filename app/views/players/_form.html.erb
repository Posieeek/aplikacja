<%= form_with(model: player, local: true) do |form| %>
  <% if player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Imię %>
    <%= form.text_field :name, id: :player_name %>
  </div>

  <div class="field">
    <%= form.label :Nazwisko %>
    <%= form.text_field :lastname, id: :player_lastname %>
  </div>

  <div class="field">
    <%= form.label :Bramki %>
    <%= form.number_field :goals, id: :player_goals %>
  </div>

  <div class="field">
    <%= form.label :Wart_mln_dol %>
    <%= form.text_field :worth, id: :player_worth %>
  </div>

  <div class="field">
    <%= form.label :Klub_id %>
    <%= form.collection_select :club_id, Club.order(:name), :id, :name %>
  </div>
  


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
